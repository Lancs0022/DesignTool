package dessinables.elementsplan;

import dessinables.geometrie.Point;

public class Fenetre extends ElementDuPlan {
    public Fenetre(Point pointDepart, double largeur, double hauteur, String nom, Piece parent) {
        super(pointDepart, largeur, hauteur, nom, parent);
    }
}
